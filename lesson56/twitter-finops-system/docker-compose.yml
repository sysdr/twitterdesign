version: '3.8'

services:
  finops-api:
    build: .
    ports:
      - "4000:4000"
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=4000
      - UI_PORT=3000
    volumes:
      - ./src:/app/src
      - ./data:/app/data
